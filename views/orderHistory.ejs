<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Orders</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    body { background: #F6FAFE; }
    .navbar { box-shadow: 0 3px 6px rgba(0,0,0,0.04); }
    .order-card {
      border-radius: 15px;
      border: none;
      box-shadow: 0 3px 8px rgba(0,0,0,0.03);
      transition: box-shadow 0.3s, transform 0.3s;
      background: #fff;
      overflow: hidden;
      position: relative;
    }
    .order-card:hover {
      box-shadow: 0 8px 24px rgba(0,0,0,0.12);
      transform: translateY(-3px) scale(1.03);
    }
    .card-body {
      padding: 18px 15px 15px 15px;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }
    .card-title {
      font-size: 1.15rem;
      font-weight: 700;
      color: #222;
    }
    .price {
      font-size: 1.05rem;
      font-weight: 600;
      color: #2C98F0;
    }
    .status {
      font-weight: 600;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <%- include('partials/navbar', { user: user, cart: cart }) %>

  <!-- Main Content -->
  <div class="container my-5 px-3">
    <h2 class="text-center fw-bold mb-5">Your Orders</h2>

    <% if (orders.length === 0) { %>
      <p class="text-center text-muted">No orders found.</p>
    <% } else { %>
      <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        <% orders.forEach(order => { %>
          <div class="col d-flex">
            <div class="card order-card h-100 w-100 shadow-sm">
              <div class="card-body d-flex flex-column">
                <h5 class="card-title">Order #<%= order.id %></h5>
                <p class="price">Total: $<%= order.total %></p>
                <p class="text-muted">Date: <%= order.order_date %></p>
                <p class="status text-primary">Status: <%= order.status %></p>
                <a href="/orders/<%= order.id %>" class="btn btn-primary mt-auto w-100">View Details</a>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

</body>
</html>
