<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Success</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/theme.css">
  <style>
    body {
      background: radial-gradient(circle at 10% 20%, #e8f0ff 0, #f7f9fc 28%, #eef2f6 60%);
      font-family: 'Segoe UI', sans-serif;
      color: #0f172a;
    }
    .card {
      max-width: 680px;
      margin: 60px auto;
      border-radius: 18px;
      border: 1px solid #e2e8f0;
      box-shadow: 0 10px 24px rgba(15, 23, 42, 0.08);
    }
  </style>
</head>
<body>
  <%- include('partials/navbar', { user: user, cart: cart }) %>

  <div class="card p-4 text-center">
    <h2 class="mb-2">Stripe Payment Successful</h2>
    <p class="text-muted mb-3">Your payment is confirmed. We’re preparing your invoice.</p>
    <p class="mb-4">Order #<%= orderId %></p>
    <div class="d-flex gap-2 justify-content-center">
      <a href="<%= invoiceUrl %>" class="btn btn-primary">View Invoice</a>
      <a href="/orders" class="btn btn-outline-secondary">Go to Orders</a>
    </div>
  </div>

  <script>
    const invoiceUrl = <%- JSON.stringify(invoiceUrl || '') %>;
    if (invoiceUrl) {
      setTimeout(() => {
        window.location.href = invoiceUrl;
      }, 2500);
    }
  </script>
</body>
</html>
