<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    body { background: #F6FAFE; }
    .navbar { box-shadow: 0 3px 6px rgba(0,0,0,0.04); }

    .order-item-card {
      border-radius: 15px;
      border: none;
      box-shadow: 0 3px 8px rgba(0,0,0,0.03);
      transition: box-shadow 0.3s, transform 0.3s;
      background: #fff;
      overflow: hidden;
      display: flex;
      gap: 15px;
      padding: 15px;
      align-items: center;
    }
    .order-item-card:hover {
      box-shadow: 0 8px 24px rgba(0,0,0,0.12);
      transform: translateY(-3px) scale(1.02);
    }
    .order-item-image {
      width: 80px;
      height: 80px;
      object-fit: contain;
      background: #F3F5F9;
      border-radius: 10px;
      padding: 5px;
    }
    .order-item-info {
      flex: 1;
    }
    .order-item-title {
      font-weight: 600;
      font-size: 1.1rem;
      color: #222;
      margin-bottom: 4px;
    }
    .order-item-details {
      font-size: 0.95rem;
      color: #555;
    }
    .order-total {
      font-size: 1.25rem;
      font-weight: 700;
      color: #2C98F0;
      margin-top: 20px;
      text-align: right;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <%- include('partials/navbar', { user: user, cart: cart }) %>

  <!-- Main Content -->
  <div class="container my-5 px-3">
    <h2 class="text-center fw-bold mb-4">Order #<%= order[0] ? order[0].order_id : '' %> Details</h2>

    <% if (!order || order.length === 0) { %>
      <p class="text-center text-muted">No items found in this order.</p>
    <% } else { %>

      <% let grandTotal = 0; %>
      <div class="row g-3">
        <% order.forEach(item => { 
          let lineTotal = item.quantity * item.price_each;
          grandTotal += lineTotal;
        %>
          <div class="col-12">
            <div class="order-item-card">
              <img src="/images/<%= item.image %>" alt="<%= item.productName %>" class="order-item-image">
              <div class="order-item-info">
                <div class="order-item-title"><%= item.productName %></div>
                <div class="order-item-details">
                  Quantity: <%= item.quantity %> | Price Each: $<%= item.price_each %> | Total: $<%= lineTotal %>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      </div>

      <div class="order-total">Grand Total: $<%= grandTotal %></div>

    <% } %>
  </div>

</body>
</html>
