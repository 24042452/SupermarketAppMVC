<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/theme.css">
  <title><%= title || 'Payment Error' %></title>
  <style>
    body {
      background: #faf7f2;
      font-family: 'Segoe UI', sans-serif;
      color: #111827;
    }
    .error-card {
      background: #fff;
      border-radius: 16px;
      padding: 24px;
      border: 1px solid #ececec;
      box-shadow: 0 10px 24px rgba(17, 24, 39, 0.06);
    }
  </style>
</head>
<body>
  <div class="container my-5" style="max-width: 720px;">
    <div class="error-card text-center">
      <h2 class="mb-3"><%= title || 'Payment Error' %></h2>
      <p class="text-muted mb-2">Response code: <%= responseCode || 'N.A.' %></p>
      <% if (instructions) { %>
        <p class="text-muted small"><%= instructions %></p>
      <% } %>
      <p class="text-danger fw-bold"><%= errorMsg || 'Transaction failed. Please try again.' %></p>
      <a href="/checkout" class="btn btn-outline-secondary">Back to checkout</a>
    </div>
  </div>
</body>
</html>
